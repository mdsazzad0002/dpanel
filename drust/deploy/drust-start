#!/usr/bin/env bash
set -euo pipefail

DRUST_BIN="/var/www/drust/target/release/drust"
if [[ ! -x "$DRUST_BIN" ]]; then
  DRUST_BIN="/var/www/drust/target/debug/drust"
fi

exec "$DRUST_BIN" \
  --port "${DRUST_API_PORT:-9500}" \
  --token "${DRUST_API_TOKEN}"
