# ServerPanel Roundcube SSO

This plugin is deployed with the Roundcube configuration bundle from `dscript/`.
It preserves the plugin layout expected by Roundcube while the panel handles the
actual login handoff.
